local temp = {}

function temp.load(last)
    love.graphics.setBackgroundColor(35,35,35)
end

function temp.update(dt)
end

function temp.draw()
end

function temp.mousepressed(x, y, button)
end
function temp.mousereleased(x, y, button)
end

function temp.keypressed(key)
end
function temp.keyreleased(key)
end

function temp.wheelmoved(x, y)
end

return temp